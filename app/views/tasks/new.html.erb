<h1>タスク新規作成ページ</h1>

<%= form_for(@task) do |f| %>
    <%= f.label :taskname, 'タスク名：' %>
    <%= f.text_field :taskname %><BR>
      <%= f.label :term, '期限：' %>
    <%= f.date_select :term %><BR>
    <BR>
    <%= f.submit '追加' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>